{
  "adapter_id": "semantic-browser-patch-only",
  "label": "Semantic Browser Patch-Only Adapter",
  "contract_version": "1",
  "target_root": ".",
  "capabilities": {
    "prepare_target": { "command": "node ../executor.cjs browser-patch-only" },
    "apply_change": { "command": "node ../executor.cjs browser-patch-only" },
    "run_target": { "command": "node ../executor.cjs browser-patch-only" }
  },
  "verification_provider": {
    "provider_id": "semantic-browser-patch-only-verifier",
    "capabilities": {
      "capture_evidence": { "command": "node ../verifier.cjs browser-patch-only" },
      "run_checks": { "command": "node ../verifier.cjs browser-patch-only" },
      "grade_round": { "command": "node ../verifier.cjs browser-patch-only" }
    }
  }
}
