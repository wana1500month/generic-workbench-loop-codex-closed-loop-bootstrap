{
  "adapter_id": "semantic-patch-only-success",
  "label": "Semantic Patch-Only Success Adapter",
  "contract_version": "1",
  "target_root": ".",
  "capabilities": {
    "prepare_target": { "command": "node ../executor.cjs patch-only-success" },
    "apply_change": { "command": "node ../executor.cjs patch-only-success" },
    "run_target": { "command": "node ../executor.cjs patch-only-success" }
  },
  "verification_provider": {
    "provider_id": "semantic-patch-only-success-verifier",
    "capabilities": {
      "capture_evidence": { "command": "node ../verifier.cjs patch-only-success" },
      "run_checks": { "command": "node ../verifier.cjs patch-only-success" },
      "grade_round": { "command": "node ../verifier.cjs patch-only-success" }
    }
  }
}
