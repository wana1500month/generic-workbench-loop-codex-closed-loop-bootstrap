{
  "adapter_id": "semantic-fullstack-patch-only",
  "label": "Semantic Fullstack Patch-Only Adapter",
  "contract_version": "1",
  "target_root": ".",
  "capabilities": {
    "prepare_target": { "command": "node ../executor.cjs fullstack-patch-only" },
    "apply_change": { "command": "node ../executor.cjs fullstack-patch-only" },
    "run_target": { "command": "node ../executor.cjs fullstack-patch-only" }
  },
  "verification_provider": {
    "provider_id": "semantic-fullstack-patch-only-verifier",
    "capabilities": {
      "capture_evidence": { "command": "node ../verifier.cjs fullstack-patch-only" },
      "run_checks": { "command": "node ../verifier.cjs fullstack-patch-only" },
      "grade_round": { "command": "node ../verifier.cjs fullstack-patch-only" }
    }
  }
}
