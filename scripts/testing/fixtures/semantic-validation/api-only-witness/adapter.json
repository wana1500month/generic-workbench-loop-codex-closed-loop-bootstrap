{
  "adapter_id": "semantic-api-only-witness",
  "label": "Semantic API-only Witness Adapter",
  "contract_version": "1",
  "target_root": ".",
  "capabilities": {
    "prepare_target": { "command": "node ../executor.cjs api-only-witness" },
    "apply_change": { "command": "node ../executor.cjs api-only-witness" },
    "run_target": { "command": "node ../executor.cjs api-only-witness" }
  },
  "verification_provider": {
    "provider_id": "semantic-api-only-witness-verifier",
    "capabilities": {
      "capture_evidence": { "command": "node ../verifier.cjs api-only-witness" },
      "run_checks": { "command": "node ../verifier.cjs api-only-witness" },
      "grade_round": { "command": "node ../verifier.cjs api-only-witness" }
    }
  }
}
