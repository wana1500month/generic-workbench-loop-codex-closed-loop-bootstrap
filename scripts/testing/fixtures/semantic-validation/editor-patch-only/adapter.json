{
  "adapter_id": "semantic-editor-patch-only",
  "label": "Semantic Editor Patch Only Adapter",
  "contract_version": "1",
  "target_root": ".",
  "capabilities": {
    "prepare_target": { "command": "node ../executor.cjs editor-patch-only" },
    "apply_change": { "command": "node ../executor.cjs editor-patch-only" },
    "run_target": { "command": "node ../executor.cjs editor-patch-only" }
  },
  "verification_provider": {
    "provider_id": "semantic-editor-patch-only-verifier",
    "capabilities": {
      "capture_evidence": { "command": "node ../verifier.cjs editor-patch-only" },
      "run_checks": { "command": "node ../verifier.cjs editor-patch-only" },
      "grade_round": { "command": "node ../verifier.cjs editor-patch-only" }
    }
  }
}
