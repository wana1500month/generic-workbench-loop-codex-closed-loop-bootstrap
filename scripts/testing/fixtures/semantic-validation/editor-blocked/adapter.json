{
  "adapter_id": "semantic-editor-blocked",
  "label": "Semantic Editor Blocked Adapter",
  "contract_version": "1",
  "target_root": ".",
  "capabilities": {
    "prepare_target": { "command": "node ../executor.cjs editor-blocked" },
    "apply_change": { "command": "node ../executor.cjs editor-blocked" },
    "run_target": { "command": "node ../executor.cjs editor-blocked" }
  },
  "verification_provider": {
    "provider_id": "semantic-editor-blocked-verifier",
    "capabilities": {
      "capture_evidence": { "command": "node ../verifier.cjs editor-blocked" },
      "run_checks": { "command": "node ../verifier.cjs editor-blocked" },
      "grade_round": { "command": "node ../verifier.cjs editor-blocked" }
    }
  }
}
