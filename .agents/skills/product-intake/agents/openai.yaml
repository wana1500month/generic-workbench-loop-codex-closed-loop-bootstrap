interface:
  display_name: "Product Intake"
  short_description: "Ask only the staged product questions for product-build work"
  default_prompt: "Use $product-intake as the staged intake gate inside $app-builder-loop, and ask only the missing product or execution questions."
