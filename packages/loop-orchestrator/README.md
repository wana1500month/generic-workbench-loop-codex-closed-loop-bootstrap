# packages/loop-orchestrator

This package orchestrates planner, generator, evaluator, controller, and optional adapter capability execution for closed-loop runs.

Keep round state file-based so runs remain reviewable and reproducible.
